1
"""CREATE TABLE "Users" (
	"Uid"	INTEGER,
	"Email"	TEXT NOT NULL UNIQUE,
	"PassHash"	TEXT NOT NULL,
	"PassSalt"	TEXT NOT NULL,
	"DisplayName"	TEXT NOT NULL,
	"Bio"	TEXT,
	PRIMARY KEY("Uid")
);"""
2
"""CREATE TABLE "Tags" (
	"Tid"	INTEGER,
	"TagName"	TEXT NOT NULL,
	PRIMARY KEY("Tid")
);'''
3
'''CREATE TABLE "UsersTag" (
	"Uid"	INTEGER,
	"Mbti"	TEXT,
	"Tid1"	INTEGER,
	"Tid2"	INTEGER,
	"Tid3"	INTEGER,
	"Tid4"	INTEGER,
	FOREIGN KEY("Tid1") REFERENCES "Tags"("Tid"),
	FOREIGN KEY("Tid3") REFERENCES "Tags"("Tid"),
	FOREIGN KEY("Tid4") REFERENCES "Tags"("Tid"),
	FOREIGN KEY("Uid") REFERENCES "Users"("Uid"),
	FOREIGN KEY("Tid2") REFERENCES "Tags"("Tid"),
	PRIMARY KEY("Uid")
);"""
4
"""CREATE TABLE "Postings" (
	"Pid"	INTEGER,
	"Detail"	TEXT NOT NULL,
	"Uid"	INTEGER NOT NULL,
	FOREIGN KEY("Uid") REFERENCES "Users"("Uid"),
	PRIMARY KEY("Pid")
);"""
5
"""CREATE TABLE "Reports" (
	"Rid"	INTEGER,
	"ReporterUid"	INTEGER NOT NULL,
	"ReportedUid"	INTEGER NOT NULL,
	"Header"	TEXT NOT NULL,
	"Detail"	TEXT,
	"Status"	INTEGER NOT NULL,
	FOREIGN KEY("ReporterUid") REFERENCES "Users"("Uid"),
	FOREIGN KEY("ReportedUid") REFERENCES "Users"("Uid"),
	PRIMARY KEY("Rid")
);"""
6
"""CREATE TABLE "Blacklists" (
	"Uid"	INTEGER,
	"Amount"	INTEGER NOT NULL DEFAULT 1,
	"Status"	INTEGER NOT NULL DEFAULT 1,
	PRIMARY KEY("Uid"),
	FOREIGN KEY("Uid") REFERENCES "Users"("Uid")
);"""